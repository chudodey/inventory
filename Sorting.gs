function sortByColumns() {
  equipmentSheet.sort(2);
  equipmentSheet.sort(3);
  equipmentSheet.sort(4);
  equipmentSheet.sort(8);
}

function sortByColumns_descending() {
  equipmentSheet.sort(2,false);
  equipmentSheet.sort(3,false);
  equipmentSheet.sort(4,false);
  equipmentSheet.sort(8,false);
}